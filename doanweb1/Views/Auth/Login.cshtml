@model doanweb1.DTO.LoginDTO;

@{
    ViewBag.Title = "Login";
}

                        @* ham,    controller,  *@
@* 
@using (Html.BeginForm("Login", "Auth", FormMethod.Post))
{
    <fieldset>
        <legend>Mvc Simple Login Application Demo</legend>

        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)

        @if (ViewBag.Message != null)
        {
            <p style="border: 1px solid red">
                @ViewBag.Message
            </p>
        }
       
        <table>
            <tr>
                <td>@Html.LabelFor(a => a.Username)</td>
                <td>@Html.TextBoxFor(a => a.Username)</td>
                <td>@Html.ValidationMessageFor(a => a.Username)</td>
            </tr>
            <tr>
                <td>@Html.LabelFor(a => a.Password)</td>
                <td>@Html.PasswordFor(a => a.Password)</td>
                <td>@Html.ValidationMessageFor(a => a.Password)</td>
            </tr>
            <tr>
                <td></td>
                <td><input type="submit" value="Login" /></td>
                <td></td>
            </tr>
        </table>
    </fieldset>
}
 *@







<!DOCTYPE html>
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <link rel="stylesheet" href="index.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
    <link
</head>

<body>
    <div>
        <section class="vh-100">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-sm-6 text-black">
                

                        <div class="d-flex align-items-center h-custom-2 px-5 ms-xl-4 mt-5 pt-5 pt-xl-0 mt-xl-n5">

                            
                          @*   @using (Html.BeginForm("Login", "Auth", FormMethod.Post))
                            {
                            <fieldset>
                            <legend>Mvc Simple Login Application Demo</legend>

                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary(true)

                            @if (ViewBag.Message != null)
                            {
                            <p style="border: 1px solid red">
                            @ViewBag.Message
                            </p>
                            }

                            <table>
                            <tr>
                            <td>@Html.LabelFor(a => a.Username)</td>
                            <td>@Html.TextBoxFor(a => a.Username)</td>
                            <td>@Html.ValidationMessageFor(a => a.Username)</td>
                            </tr>
                            <tr>
                            <td>@Html.LabelFor(a => a.Password)</td>
                            <td>@Html.PasswordFor(a => a.Password)</td>
                            <td>@Html.ValidationMessageFor(a => a.Password)</td>
                            </tr>
                            <tr>
                            <td></td>
                            <td><input type="submit" value="Login" /></td>
                            <td></td>
                            </tr>
                            </table>
                            </fieldset>
                            }
                            * *@
                           

                            <form action="/Auth/Login" method="post" style="width: 23rem;">

                                <h3 class="fw-normal mb-3 pb-3" style="letter-spacing: 1px;">Log in</h3>

                                <div class="form-outline mb-4">
                                    @Html.TextBoxFor(a => a.Username, new { @class = "form-control form-control-lg ", @id = "form2Example18" })

                                  @*   <input name="Username" type="text" id="form2Example18" class="form-control form-control-lg" /> *@
                                    <label class="form-label" for="form2Example18">Username</label>
                                </div>

                                <div class="form-outline mb-4">
                                    @Html.PasswordFor(a => a.Password, new { @class = "form-control form-control-lg ", @id = "form2Example18" })
                                    @* <input  name="Password" type="password" id="form2Example28" class="form-control form-control-lg" /> *@
                                    <label class="form-label" for="form2Example28">Password</label>
                                </div>
                                @Html.ValidationSummary(false, "", new { @class = "validation-summary-errors" })
                                <div class="pt-1 mb-4">
                                    <input type="submit" value="Login" class="btn btn-info btn-lg btn-block " />
                                   @*  <button class="btn btn-info btn-lg btn-block" type="button">Login</button> *@
                                </div>

                                <p class="small mb-5 pb-lg-2"><a class="text-muted" href="/Auth/Forgotpassword">Forgot password?</a></p>
                                <p>Don't have an account? <a href="/Auth/Register" class="link-info">Register here</a></p>

                            </form>

                        </div>

                    </div>
                    <div class="col-sm-6 px-0 d-none d-sm-block">
                        <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-login-form/img3.webp"
                             alt="Login image" class="w-100 vh-100" style="object-fit: cover; object-position: left;">
                    </div>
                </div>
            </div>
        </section>
    </div>
</body>
</html>